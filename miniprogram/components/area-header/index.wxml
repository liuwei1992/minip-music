<!--components/area-header/index.wxml-->
<view class="areaHeader">
  <text class="title">{{ title }}</text>
  <view wx:if="{{hasMore}}" class="more" bindtap="onMoreTap">
    <text>更多</text>
    <van-icon name="arrow" />
  </view>
</view>